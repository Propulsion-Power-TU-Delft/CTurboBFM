#include "turbulence_model_sa.hpp"

TurbulenceModelSA::TurbulenceModelSA(const Config &config, const FluidBase &fluid, const Mesh &mesh) 
    : TurbulenceModelBase(config, fluid, mesh) {
        
    }